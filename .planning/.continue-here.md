---
context: default
phase: workspace-console-refinement
task: 5
total_tasks: 5
status: paused
last_updated: 2026-04-18T13:35:00+09:00
---

## Current State

Paused after completing the broad workspace layout refinement pass and the customer-management restructuring follow-up.

Completed in this session:
- converted the workspace screens toward a wider console layout with reduced explanatory copy
- moved sales and receivables screens to top filter bars, icon actions, and modal-driven detail flows
- unified custom select/date popovers so expanded lists render above surrounding panels
- normalized input/select trigger heights across the workspace
- added dashboard top filters and tightened date-picker/modal placement behavior
- rebuilt `/customers` so filters auto-apply, widths are tighter, and customer detail / sales history / receivable history open from icon actions in modals

## Completed Work

Completed Tasks:
- Task 1: Workspace-wide console layout and density pass - Done
- Task 2: Sales and receivables workflow restructuring - Done
- Task 3: Dashboard/report layout and filter refinement - Done
- Task 4: Shared control polish (popover layering, heights, modal placement) - Done
- Task 5: Customer-management auto filters and modal detail flows - Done

## Remaining Work

- Run a broader final UI QA pass across all menus using the current webpack dev server
- Decide whether to keep or remove the remaining metrics/helper cards that still consume vertical space
- Resolve the local Next 16 Turbopack Windows junction issue if Playwright must run against Turbopack instead of webpack

## Decisions Made

- Keep main lists visible on the page and move drill-in detail into modals instead of persistent side panels
- Use query-driven modal state for `/customers` so server-rendered data stays authoritative and refresh-safe
- Keep customer duplicate prevention based on phone-number duplication only
- Prefer top filter bars with auto-apply over explicit apply/reset buttons in operational list screens

## Blockers

- `pnpm playwright test tests/e2e/home.spec.ts` is still blocked when routed through the local Turbopack web server because of the Windows junction recreation error around `.next/dev/node_modules/better-sqlite3-*`

## Required Reading
1. `PROJECT_SETUP_REPORT.md`
2. `PHONE_SHOP_WORK_PLAN.md`
3. `.planning/HANDOFF.json`
4. `PROJECT_DOCS.md`

## Latest Validation

- `pnpm typecheck`
- `pnpm exec eslint "src/app/(workspace)/customers/page.tsx" "src/components/workspace/customers-overview.tsx" "src/components/workspace/customers-filter-bar.tsx" "src/components/workspace/query-modal-shell.tsx" "src/lib/customers-url-state.ts"`
- manual browser verification on:
  - `/`
  - `/sales`
  - `/receivables`
  - `/customers`
  - `/inventory`
  - `/settings/base`
  - `/settings/policies`

## Next Action

Start with a quick global QA pass on the current webpack-based dev server, then decide whether the next slice is pure polish or Turbopack/Playwright environment cleanup.
