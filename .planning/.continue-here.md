---
context: default
phase: schedule-crud-and-validation-stabilization
task: 4
total_tasks: 4
status: paused
last_updated: 2026-04-20T17:18:00+09:00
---

## Current State

Paused after verifying the no-key holiday source live in `/schedule` and rechecking direct `pnpm dev` self-start E2E.

Completed in this session:
- completed manual schedule create/update/delete flows and dialog wiring from the calendar
- added focused schedule action/unit coverage and `tests/e2e/schedule.spec.ts`
- hardened `src/lib/prisma.ts` against stale dev-session Prisma clients missing the `manualSchedule` delegate
- configured Playwright to run serially when reusing an external dev server through `PLAYWRIGHT_BASE_URL`
- reverified `home`, `modal-a11y`, and `schedule` against a fresh webpack dev server on `http://localhost:3101`
- switched holiday fetching to no-key public sources and verified live holiday rendering on `/schedule?month=2026-05`
- re-ran the direct `pnpm dev` self-start Playwright path successfully

## Completed Work

Completed Tasks:
- Task 1: `ManualSchedule` Prisma schema, migration, and seed updates - Done
- Task 2: Holiday API client with cache fallback - Done
- Task 3: Schedule page-data aggregation and month calendar / summary panel UI - Done
- Task 4: Refresh webpack dev server and rerun `home` / `modal-a11y` regressions - Done

## Remaining Work

- Resume deployment-readiness and external environment work

## Decisions Made

- Keep automatic schedule events computed at read time instead of persisting them
- Use year-scoped holiday fetching with cache fallback so API outages do not block `/schedule`
- Rebuild the cached Prisma client when a stale dev-session instance is missing the current schema's `manualSchedule` delegate
- Reuse a fresh webpack dev server through `PLAYWRIGHT_BASE_URL`, but force Playwright to run serially against the shared SQLite DB

## Blockers

- Direct `pnpm playwright test tests/e2e/home.spec.ts tests/e2e/modal-a11y.spec.ts tests/e2e/schedule.spec.ts` passed again with `pnpm dev` self-start on 2026-04-20.
- `/schedule?month=2026-05` now serves as the current live-data check for no-key holiday rendering.

## Required Reading
1. `PROJECT_SETUP_REPORT.md`
2. `PHONE_SHOP_WORK_PLAN.md`
3. `.planning/STATE.md`
4. `PROJECT_DOCS.md`

## Latest Validation

- `pnpm prisma migrate dev --name add_manual_schedule`
- `pnpm prisma generate`
- `pnpm prisma db seed`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm build`
- `pnpm vitest run src/lib/holidays.test.ts src/lib/schedule.test.ts src/app/actions/schedules.test.ts src/components/workspace/workspace-nav.test.tsx`
- `pnpm playwright test tests/e2e/home.spec.ts tests/e2e/modal-a11y.spec.ts tests/e2e/schedule.spec.ts`
- `PLAYWRIGHT_BASE_URL=http://localhost:3101 pnpm playwright test tests/e2e/home.spec.ts tests/e2e/modal-a11y.spec.ts tests/e2e/schedule.spec.ts`

## Next Action

Resume deployment-readiness and external environment setup work.
